Module example1

Def y: 1 + 3 - 99828932 / 2 + 2 - 1;

DEF x: 12 / 3 - 1;

x * 2 - 4;

def t: 4;

DEF func(t, x):
    t * t * t + x;

// This language is case-insensitive regarding symbol names
Func(T, X);
Func(X, T);
